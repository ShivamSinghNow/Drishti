# Drishti Landing Page

Static implementation of the Claude Design handoff for `Drishti Landing.html`.

Open `index.html` directly in a browser:

```bash
open frontend/index.html
```

The page includes the phone-first hero, smooth section navigation, dark mode, an interactive inference demo, evaluation cards, stack credits, and install/setup snippets.
